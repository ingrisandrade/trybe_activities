const { it, expect } = require("@jest/globals");
const { describe } = require("./summationOf");

describe('', () => {
  it('Função existe', () => {
    expect(typeof summationOf).toBe('function');
  });
});